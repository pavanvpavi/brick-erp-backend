# # spring.application.name=brick-erp-backend

# # server.port=${PORT:8080}
# # server.address=0.0.0.0

# # spring.datasource.url=${SPRING_DATASOURCE_URL}
# # spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
# # spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
# # spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # spring.jpa.hibernate.ddl-auto=update
# # spring.jpa.show-sql=true
# # spring.jpa.properties.hibernate.format_sql=true
# # spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# # spring.jackson.serialization.write-dates-as-timestamps=false
# # spring.jackson.time-zone=UTC

# # logging.level.com.brickerp=DEBUG
# # logging.level.org.springframework.security=INFO

# spring.application.name=brick-erp-backend

# server.port=${PORT:8080}
# server.address=0.0.0.0

# spring.datasource.url=${SPRING_DATASOURCE_URL}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# spring.jackson.serialization.write-dates-as-timestamps=false
# spring.jackson.time-zone=UTC

# app.jwt.secret=${APP_JWT_SECRET:BrickERPSecretKey2024BrickERPSecretKey2024BrickERPSecretKey2024}
# app.jwt.expiration=86400000

# logging.level.com.brickerp=INFO
# logging.level.org.springframework.security=INFO

# spring.mail.host=${MAIL_HOST:smtp.gmail.com}
# spring.mail.port=${MAIL_PORT:587}
# spring.mail.username=${MAIL_USERNAME:your-email@gmail.com}
# spring.mail.password=${MAIL_PASSWORD:your-app-password}
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# app.alert.email=${ALERT_EMAIL:admin@brickerp.com}

spring.application.name=brick-erp-backend

# Server
server.port=${PORT:8080}
server.address=0.0.0.0

# Database
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JSON
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC

# JWT
app.jwt.secret=${APP_JWT_SECRET}
app.jwt.expiration=86400000

# Email alert receiver
app.alert.email=${ALERT_EMAIL}

# Gmail SMTP
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Logging
logging.level.com.brickerp=INFO
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=DEBUG